name: calculator
description: A simple Flutter calculator app

publish_to: "none" # for local apps

version: 1.0.0+1

environment:
  sdk: '^3.9.0'

dependencies:
  flutter:
    sdk: flutter
  math_expressions: ^2.5.0

dev_dependencies:
  flutter_test:
    sdk: flutter
